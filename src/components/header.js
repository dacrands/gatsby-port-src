import React from 'react'
import { Link } from 'gatsby'

const Header = ({ siteTitle }) => (
  <div>
    <div className="landing">
      <h1>Watch Blog</h1>
      <p>This is a site where I 
        write about watches.
      </p>
      </div>
  </div>
)

export default Header
